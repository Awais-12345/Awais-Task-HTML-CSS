const age = 20;
switch(age){
     case (age<=25 && age>=30):
        console.log("you are eligible for deriving");
        break;
        case(age<=15 && age>=18):
        console.log("you are not eligible for deriving");
        break;
        default:
            console.log("otherwise wrong age");
     }
